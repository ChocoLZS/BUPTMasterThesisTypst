= 总结与使用建议

模板将结构和样式分离：个人信息集中写在 `main.typ`，章节正文放在 `chapters` 目录，摘要与后置内容分别放在 `frontmatter` 和 `backmatter` 目录。作者不需要在正文中设置页边距、页码、页眉、字号或标题格式。

正式提交前，应使用当前版本 Typst 重新编译全文，并检查封面信息、目录页码、图表编号、参考文献以及双面打印时的空白页。若学院发布了更新要求，可优先通过 `main.typ` 中的 `settings` 参数调整全局版式。

= 模板使用示例

本章展示论文正文中的常用结构。标题会自动编号，正文默认采用小四号字、20 磅固定行距和首行缩进，无需在章节文件中重复设置。

== 标题与正文

作者通常只需要使用 Typst 的标题语法组织内容。正文中的中英文、数字和标点会按照模板设置自动选择字体并完成两端对齐。

=== 三级标题

三级标题采用小四号黑体。目录默认收录到三级，深度可在 `main.typ` 中修改。

==== 四级标题

四级标题采用小四号宋体，建议避免过深的层级。

== 图、表与公式